import { ChatList } from '@/features/chats/list/components/chat-list';

export default function ChatsPage() {
  return (
    <main className="flex flex-1 flex-col">
      <ChatList />
    </main>
  );
}
