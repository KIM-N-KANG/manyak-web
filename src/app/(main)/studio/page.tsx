import { CreatedStoryList } from '@/features/studio/menu/components/created-story-list';

export default function StudioPage() {
  return (
    <main className="flex flex-1 flex-col">
      <CreatedStoryList />
    </main>
  );
}
